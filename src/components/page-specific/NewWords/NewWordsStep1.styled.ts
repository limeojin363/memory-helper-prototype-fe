import styled from "@emotion/styled";

const S = {
    Root: styled.div`
        display: flex;
        flex-direction: column;

        gap: 8px;
    `,
    WordInputPairItemWrapper: styled.div``,
};

export default S;