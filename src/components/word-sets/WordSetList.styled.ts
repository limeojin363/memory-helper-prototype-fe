import styled from "@emotion/styled";

const S = {
    Root: styled.div`
        display: flex;
        flex-direction: column;
        gap: 8px;
        .ghost {
            opacity: 0;
        }
    `,
};

export default S;
