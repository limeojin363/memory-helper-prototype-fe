import styled from "@emotion/styled";

const S = {
    Root: styled.div`
        position: relative;

        display: flex;
        flex-direction: column;
        gap: 8px;
    `,
};

export default S;
