import styled from "@emotion/styled";

const S = {
    PageWrapper: styled.div`
        display: flex;
        flex-direction: column;
        align-items: center;
        justify-content: center;

        width: 100vw;
    `,
    Input: styled.input``,

};

export default S;
